[package]
name = "sidecar"
version = "0.1.0"
edition.workspace = true

[dependencies]
algo-core = { path = "../algo-core" }
storage = { path = "../storage" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
